{
	"name": "workout-app",
	"version": "1.0.0",
	"description": "",
	"main": "server.js",
	"type": "module",
	"scripts": {
		"server": "nodemon back/server",
		"dev": "concurrently \"npm run server\" \"npm run start --prefix front\"",
		"start": "concurrently \"npm run server\" \"npm run start --prefix front\"",
		"build": "cd front && npm run build",
		"install-client": "cd front && npm install",
		"heroku-postbuild": "cd front && npm install && npm run build"
	},
	"author": "Max (htmllessons.ru)",
	"license": "ISC",
	"dependencies": {
		"bcryptjs": "^2.4.3",
		"concurrently": "^6.2.0",
		"dotenv": "^10.0.0",
		"express": "^4.17.1",
		"express-async-handler": "^1.1.4",
		"jsonwebtoken": "^8.5.1",
		"mongoose": "^5.13.2",
		"nodemon": "^2.0.12"
	},
	"devDependencies": {
		"colors": "^1.4.0",
		"morgan": "^1.10.0"
	}
}
